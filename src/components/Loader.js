import React from "react";
import ClipLoader from "react-spinners/ClipLoader";

const Loader = () => {
  return (
    <div>
      <ClipLoader color={"#0071bd"} size={150} />
    </div>
  );
};

export default Loader;
